import { Modal, ModalContent, ModalHeader } from "@shared/components/UI/Modal";

const CreateAndUpdateRoom = () => {
  return (
    <Modal size="lg">
      <ModalHeader title="Thêm mới" />
      <ModalContent>
        <div>sfkjsdkfjdsk</div>
      </ModalContent>
    </Modal>
  );
};

export default CreateAndUpdateRoom;
