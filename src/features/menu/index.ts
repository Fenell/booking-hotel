/** Cửa trước của feature menu — nơi khác chỉ import từ đây. */
export { useMenu } from "./useMenu";
export {
  findMenuByPath,
  isSamePath,
  toAbsolutePath,
  type MenuMatch,
} from "./menu.path";
